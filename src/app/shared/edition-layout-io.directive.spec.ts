import { EditionLayoutIoDirective } from './edition-layout-io.directive';

describe('EditionLayoutIoDirective', () => {
  it('should create an instance', () => {
    const directive = new EditionLayoutIoDirective();
    expect(directive).toBeTruthy();
  });
});
